apiVersion: v1
kind: Service
metadata:
  name: {{ include "galileo.fullname" . }}
  labels:
    {{- include "galileo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "galileo.selectorLabels" . | nindent 4 }}
